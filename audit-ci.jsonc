// audit-ci.jsonc
{
  // $schema provides code completion hints to IDEs.
  "$schema": "https://github.com/IBM/audit-ci/raw/main/docs/schema.json",
  "moderate": true,
  // Dev-only advisories don't reach consumers; prod deps are audited as normal.
  "skip-dev": true,
}
