# dx-lib backlog

## QA hooks workstream (Part A: audit + registration core + track facade)

**Origin:** dx-qa PLAN.md v1 (2026-07-03, `~/Documents/Work/dx-qa/PLAN.md` §8). dx-qa reads jf-lib's instrumentation as its primary QA signal; the estate's observability holes map exactly to missing library surface. Enforcement/rollout is dx-harness + humans, not dx-qa or this library.
**Idea:** (1) Fable audit of dx-lib, seeded with verified findings: `emitEvent` throws on malformed ticketId/variant _inside its validation_ (`src/modules/emitEvent.ts:46-54`), so error-path reporting can swallow the original error — error paths must never throw; pageChange detection is a MutationObserver on `<html>` subtree gated on meta/canonical _presence_ + pathname change (`useSPA.ts:540-551`); `watchForRemoval` has a `>= 6` / `>= 5` double boundary (`useSPA.ts:1108,1116`). (2) Design doc + prototype branch for: a registration core factored out of `useSPA` (OBF/static builds get `window.jfLib.experiments[].details` + lifecycle events without SPA machinery); a `track()` facade routing to the platform channel (monetateQ / Test.pageview / ddEvent) while always emitting a structured `jf-wx-track`; auto-tracked resources (tracked `on()`/interval helpers, lib-owned `reset()` teardown, `details.resources` count); `apply:start/done`, `reapply`, `reset:done` phases on a versioned `jf-lifecycle-1.0` event. The design doc must be grounded in consumer usage: survey the most recent builds across 2–3 clients in `~/Documents/Work/jf-conversion/client-files/`, and every proposed hook cites the hand-rolled build pattern it replaces (e.g. per-build `waitForNuxtStable`, hand-rolled `trackMetric`, untracked `addEventListener`). (3) Convert one build — ideally Sam's from-scratch build (w/c 2026-07-06) — as first consumer.
**Context for next agent:** Step (1) is DONE — see `AUDIT.md` (2026-07-03, two rounds: runtime audit + 2.0.11→HEAD delta and consistency sweep); fixes in progress. Surface dx-qa will consume: `jf-pagechange-1.0`, `jf-reinit-1.0`, `window.jfLib.experiments[].details`, `window.jfLib.pagePath`, `#<id>--style` tag convention, and the emitEvent wire events — for those, the `jf-wx-*` names are SANCTIONED FOR RENAME to the versioned family (`jf-err-1.0`/`jf-test-1.0`/`jf-track-1.0`, per AUDIT.md Round 2; "wx" is a retired team name and dx-qa is pre-build so it's a clean break on that side). The real constraints: the existing canary tool listens for `jf-wx-*` (update in lockstep or dual-dispatch for one deprecation window), and 2.0.11 bundles in deployed/historical experiments emit the old names forever — dx-qa listens for both families. jf-conversion's `2.0.11` pin is expected to be lifted, but the estate stays heterogeneous (old inlined bundles), so version-stamp the new surface (capability flags preferred over version sniffing; note 2.0.11 `details` is only `{isRunning, id}` and `experiments[]` holds raw STATE, not `publicApi`). Part B (measuring heuristic→deterministic check delta on a converted vs unconverted build) is blocked on dx-qa `run_flow` v1 existing; that delta is the merge/rollout business case. Ticket-prefixed element classes are per-build discipline today, not a lib convention — the registration core is the fix, not enforcing the class convention.

## General DX helper mining from jf-conversion builds

**Origin:** dx-qa planning session (2026-07-03) — while designing QA hooks, noted that repeated hand-rolled patterns across experiment builds are helper-extraction candidates beyond the QA-relevant ones. Kept separate from the QA hooks workstream: different consumer (build velocity, not QA observability), different judge (experiment devs / dx-harness codegen templates), independent timing.
**Idea:** Survey recent builds across clients in `~/Documents/Work/jf-conversion/client-files/` for patterns copy-pasted per build that have no QA angle — DOM utilities, formatting, viewport/scope checks, whatever repeats — and propose dx-lib helpers for the ones that earn it. dx-harness changes the economics: once codegen templates are the enforcement point, an extracted helper propagates to every new build for free.
**Context for next agent:** QA-relevant helpers (hydration wait, tracking facade, tracked listeners/teardown, registration) are explicitly NOT this entry — they belong to the QA hooks workstream above and its design doc. Start from the newest builds (they reflect current conventions, e.g. TOS_013244, GHD_013284, ITC_013319, FUN_013341); older builds predate current rulesets and will suggest helpers for patterns that no longer exist. Proposals should cite the builds they'd have simplified, and check `.cursor/rules/` in jf-conversion — a pattern mandated by a ruleset is a stronger extraction candidate than one that merely recurs.

## Dispose/undo contract for live re-injection (jf-lib side)

**Origin:** dx-webpack-hmr HMR landscape review (2026-07-17, `dx-webpack-hmr/audit/HMR-LANDSCAPE.md` §5.1) — the highest-value fluidity increment for the live-preview loop, but the API lives in jf-lib, not the plugin.
**Idea:** Variant code (or jf-lib DOM helpers automatically) registers cleanup — `jfLib.onDispose(fn)`, or helpers that record undo ops as they mutate. The dev plugin can then run cleanup and inject the rebuilt bundle in place instead of reloading the page: fluid iteration, and each avoided reload is one less Cloudflare-visible page load. AI-first development makes the convention enforceable — agents can be required to register cleanup in every variant via rulesets/codegen templates, removing the human-discipline blocker that killed this idea historically.
**Context for next agent:** The plugin side is ready to consume it: injection goes through `addCompiledBundle`/`addCodeToPuppeteerDOM` (script-tag replace), so the change is roughly "if `window.jfLib?.dispose` exists, await it and inject in place; else fall back to the state-restoring reload that already shipped". This overlaps heavily with the registration-core/auto-tracked-resources design in the QA hooks entry above — lib-owned `reset()` teardown is the same mechanism; design them as one thing, not two.
